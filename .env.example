# Copie este arquivo para `.env.local` e ajuste os valores.

# URL do backend Convex
# Cloud (produção): substitua pelo seu deployment
#   Ex.: https://seu-deployment.convex.cloud
# Local (desenvolvimento): http://127.0.0.1:8187
VITE_CONVEX_URL="https://avid-jackal-629.convex.cloud"

# Opcional: identifica o deployment para a CLI do Convex
# Não é usado pelo frontend, apenas por comandos `npx convex deploy` etc.
CONVEX_DEPLOYMENT="avid-jackal-629"